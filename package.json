{
  "name": "turn-website",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && node server.js",
    "start": "node server.js",
    "preview": "npm run build && PORT=4173 node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
